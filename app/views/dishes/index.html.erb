<p id="notice"><%= notice %></p>
<body id="page-top" data-spy="scroll" data-target=".navbar-fixed-top">

  <!-- Navigation -->
  <nav class="navbar navbar-custom navbar-fixed-top" role="navigation">
      <div class="container">
          <div class="navbar-header">
              <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-main-collapse">
                  <i class="fa fa-bars"></i>
              </button>
              <a class="navbar-brand page-scroll" href="#page-top">
                  <i class="fa fa-play-circle"></i>  <span class="light">Home</span>
              </a>
          </div>

          <!-- Collect the nav links, forms, and other content for toggling -->
          <div class="collapse navbar-collapse navbar-center navbar-main-collapse">
              <ul class="nav navbar-nav navbar">
                  <!-- Hidden li included to remove active class from about link when scrolled up past about section -->
                  <li>
                      <a class="page-scroll" href="#appetizers">Appetizers</a>
                  </li>
                  <li>
                      <a class="page-scroll" href="#plates">Small Plates</a>
                  </li>
                  <li>
                      <a class="page-scroll" href="#entrees">Entrees</a>
                  </li>
                  <li>
                      <a class="page-scroll" href="#desserts">Desserts</a>
                  </li>
                  <li>
                      <a class="page-scroll" href="#drinks">Drinks</a>
                  </li>
              </ul>
          </div>
          <!-- /.navbar-collapse -->
      </div>
      <!-- /.container -->
  </nav>


  <div class="brand">Ensō</div>
  <div class="address-bar">2518 Cherry Ln | Durham, NC 27701 | 123.456.7890</div>

<!-- untitled -->


<div class="row">
  <div class="box">
    <div class="col-lg-12">
      <hr>
        <% @courses.each do |course| %><br>
        <div>
          <div>
            <h2 class="intro-text text-center">
              <%= course.name %><br>
              <h6>
                <strong><%= link_to "Add Dish", new_dish_path(course_id: course.id) %><br></strong>
              </h6>
            </h2>
            <% if course.id == 1 %>
              <section id="appetizers">
                <%= image_tag asset_url("cherry.png"), id: "course_1" %>
              </section>
            <% elsif course.id == 2 %>
              <section id="plates">
                <%= image_tag asset_url("bamboo.png"), id: "course_2" %>
              </section>
            <% elsif course.id == 3 %>
              <section id="entrees">
                <%= image_tag asset_url("torii.png"), id: "course_3" %>
              </section>
            <% elsif course.id == 4 %>
              <section id="desserts">
                <%= image_tag asset_url("tea.png"), id: "course_4" %>
              </section>
            <% elsif course.id == 5 %>
              <section id="drinks">
                <%= image_tag asset_url("enso.png"), id: "course_5" %>
              </section>
            <% end %>
          </div>
        <div>
        <% course.dishes.each do |dish| %>
        <div class="row">
          <div class="box dishes">
            <div class="col-lg-12">
              <p>$<%= dish.price %></p>
              <p><%= link_to "#{dish.name}", edit_dish_path(dish) %></p>
              <p><%= dish.description %></p>
              <p><%= link_to 'Show', edit_dish_path(dish) %></p><br>
            </div>
          </div>
        </div>
        <% end %>
    </div>
  </h4>
  </div>
  <% end %>

</div>
</div>
</div>
</body>
